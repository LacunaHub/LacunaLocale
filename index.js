const Russian = require('./translations/Russian/messages.json')

const messages = {
  ru: Russian
}

const languages = {
  ru: { name: 'Русский', code: 'ru' }
}

module.exports = {
  messages,
  languages
}
